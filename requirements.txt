# requirements.txt
Django==5.2
psycopg2-binary
gunicorn

# Celery et dépendances
celery==5.3.4
django-celery-beat>=2.6.0
redis==5.0.1
django-redis==5.4.0

# Rate limiting (s'appuie sur le cache Django ci-dessus pour compter les
# requêtes de façon partagée entre tous les workers Gunicorn)
django-ratelimit==4.1.0

# Images
Pillow>=10.0.0

# Configuration
django-environ

# Utilitaires
python-dateutil==2.8.2
pytz==2023.3

# Tests
pytest==9.1.1
pytest-django==4.14.0
